
import { beforeEach, expect, describe, test } from '@jest/globals';

describe('ClientRepo', () => {
 
  beforeEach(() => {
    //repo = new CarrinhoRepo();
  });

  test('deve registrar um cliente', () => {
    expect(true).toBe(true);

  });


});